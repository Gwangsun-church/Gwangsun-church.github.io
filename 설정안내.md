# 광선교회 홈페이지 (무료 버전) — 설정 안내

GitHub Pages + Firebase(구글 로그인·게시판) + Cloudinary(사진·주보) 구조.
월 비용 0원. 아래 값들만 채우면 모든 기능이 켜진다.

## 채워야 할 곳 (딱 2개 파일)

### 1. `js/config.js`
- `firebaseConfig` — Firebase 콘솔 > 프로젝트 설정(톱니) > 일반 > 내 앱 > 웹앱 > SDK 설정의 값
- `ADMIN_EMAILS` — 관리자 구글 계정 (예: gwangsun.church@gmail.com)
- `CLOUDINARY.cloudName` — Cloudinary 대시보드의 Cloud name
- `CLOUDINARY.uploadPreset` — 만들어 둔 unsigned 프리셋 이름 (기본 "gwangsun")

### 2. `firestore.rules`
- isAdmin() 안의 이메일을 ADMIN_EMAILS 와 동일하게 수정
- 전체 내용을 Firebase 콘솔 > Firestore Database > 규칙 탭에 붙여넣고 "게시"

## Firebase 콘솔에서 할 일 (프로젝트: gwangsun-church-board)
1. 빌드 > Authentication > 시작하기 > 로그인 방법 > Google > 사용 설정 > 저장
2. 빌드 > Firestore Database > 데이터베이스 만들기 > 위치 asia-northeast3(서울) > 프로덕션 모드
3. 프로젝트 개요 옆 톱니 > 프로젝트 설정 > 일반 > 내 앱 > 웹(</>) 앱 등록 > firebaseConfig 복사
4. Authentication > 설정 > 승인된 도메인 > `gwangsun-church.github.io` 추가
5. Firestore > 규칙 탭 > firestore.rules 내용 붙여넣고 게시

## Cloudinary 에서 할 일 (cloudinary.com 무료 가입)
1. 가입 후 대시보드에서 Cloud name 확인
2. Settings(톱니) > Upload > Upload presets > Add upload preset
   - Name: gwangsun / Signing mode: **Unsigned** > Save
3. Settings > Security > "PDF and ZIP files delivery" **허용(체크)** ← 주보 PDF 표시에 필요

## GitHub 에 올리기
1. github.com 의 `Gwangsun-church.github.io` 저장소 > Add file > Upload files
2. 이 폴더(E:\gwangsun-church-static) 안의 **모든 파일/폴더**를 끌어다 놓고 Commit
3. 저장소 Settings > Pages > Source: Deploy from a branch / Branch: main / 폴더: / (root) > Save
4. 1~2분 후 https://gwangsun-church.github.io 접속

## 관리자 사용법
- 사이트에서 "로그인" > 관리자 구글 계정으로 로그인
- 교회소식 페이지에 "주보 업로드" 폼이 자동 표시 → PDF 올리면 끝
  (Cloudinary가 쪽수를 인식해 페이지 이미지로 표시됨)

## 나중에 (선택)
- 도메인 구매 시: 저장소 Settings > Pages > Custom domain 에 도메인 입력
  + 도메인 DNS에서 CNAME → gwangsun-church.github.io
  + Firebase 승인된 도메인에도 새 도메인 추가
- 유튜브 자동 기능: config.js 의 YOUTUBE.apiKey 에 키 입력
